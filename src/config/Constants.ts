/// src/config/Constants.ts

export const MAX_ENTITIES = 65536;
export const FIXED_DT = 1 / 60;
export const CELL_SIZE = 32;
export const PLAYER_ID = 1;
// World dimensions - use the largest floor dimensions as the world bounds
export const WORLD_WIDTH = 32 * 64;  // 32 tiles * 64px (largest floor)
export const WORLD_HEIGHT = 32 * 64; // 32 tiles * 64px (largest floor)
